var config = {
  type: Phaser.AUTO,
  width: 600,
  height: 800,
  scene: {
    preload,
    create,
    update,
  },
};

var game = new Phaser.Game(config);

function preload() {}
function create() {}
function update() {}
